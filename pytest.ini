[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short
markers =
    unit: Unit tests (no external connections)
    integration: Integration tests (requires Databricks)
    slow: Tests that take more than 30 seconds
    ci_exclude: Tests excluded from CI/CD pipeline
